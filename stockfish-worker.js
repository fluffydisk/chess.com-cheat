// stockfish-worker.js — unused in current setup
// We now run `stockfish.js` directly as a Worker from `popup.js`.
// This file is left as a harmless stub to avoid missing-file errors when
// loading in environments that may still reference it. No runtime behavior.

// Intentionally empty.
